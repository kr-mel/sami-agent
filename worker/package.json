{
  "name": "sami-agent-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Sami — AI assistant agent for a sweet shop (portfolio demo). Cloudflare Worker + Gemini.",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
